{
  "$schema": "https://openapi.vercel.sh/vercel.json",

  "buildCommand": "echo 'No build on Vercel — @vercel/node compiles api/index.ts itself.'",
  "outputDirectory": "public",

  "rewrites": [{ "source": "/(.*)", "destination": "/api/index" }]
}
